# 图片生成功能快速开始指南

## 第一次使用

### 1. 准备工作

确保你已经：
- ✅ 安装了 Chrome 扩展
- ✅ 访问了 Arcaea Online 查分页面
- ✅ 安装了 Node.js（v14 或更高版本）

### 2. 导出数据

1. 打开 [Arcaea Online](https://arcaea.lowiro.com/) 并登录
2. 进入 Profile - Potential 页面
3. 等待扩展加载完所有数据（你会看到定数、PTT等信息）
4. 点击 Chrome 扩展图标 🔌
5. 在弹出窗口中找到"数据导出"部分
6. 点击下载按钮 📥
7. 保存 JSON 文件（例如：`arcaea-b30r10-2024-12-01.json`）

### 3. 安装依赖（仅首次）

打开终端，进入项目目录，运行：

```bash
npm install
```

> 如果遇到 `canvas` 包安装问题，请参考下方的系统依赖安装说明。

### 4. 生成图片

**最简单的方式（推荐）：**

```bash
npm run generate-image
```

脚本会自动：
- 🔍 搜索当前目录下的JSON文件
- ✅ 自动选择最新的有效文件（如果只有一个）
- 📋 显示数据概览
- ✨ 生成图片

**手动指定文件：**

```bash
npm run generate-image <你的JSON文件路径>
```

**具体示例：**

```bash
# 如果文件在下载文件夹
npm run generate-image ~/Downloads/arcaea-b30r10-2024-12-01.json

# 如果文件在项目根目录
npm run generate-image ./arcaea-b30r10-2024-12-01.json

# Windows 用户
npm run generate-image C:\Users\YourName\Downloads\arcaea-b30r10-2024-12-01.json
```

### 5. 查看结果

生成的图片会保存在当前目录，文件名类似：

```
arcaea-b30r10-<你的玩家名>-<时间戳>.png
```

图片包含：
- 顶部：玩家信息（用户名、总PTT、B30平均、R10平均）
- 下方：8行5列的歌曲卡片（共40张）
  - Best 30 在前6行
  - Recent 10 在后2行

---

## 系统依赖安装

### macOS

```bash
brew install pkg-config cairo pango libpng jpeg giflib librsvg
```

### Ubuntu/Debian/WSL

```bash
sudo apt-get update
sudo apt-get install build-essential libcairo2-dev libpango1.0-dev libjpeg-dev libgif-dev librsvg2-dev
```

### Windows

通常不需要额外安装，npm 会自动下载预编译的二进制文件。

如果遇到问题：
1. 安装 [Windows Build Tools](https://github.com/felixrieseberg/windows-build-tools)
2. 或参考 [node-canvas Windows 安装指南](https://github.com/Automattic/node-canvas/wiki/Installation:-Windows)

---

## 常见问题

### ❓ 我不想每次都手动输入文件路径？

**解决方案**：直接运行 `npm run generate-image`，脚本会自动搜索并选择JSON文件！

只需将导出的JSON文件放在项目根目录，然后运行命令即可。如果有多个文件，会弹出选择菜单。

### ❓ 生成的图片中没有曲绘？

这是正常的。曲绘URL可能需要登录才能访问，脚本会继续生成图片，只是背景是纯色。

### ❓ 提示"未找到JSON文件"？

请确保：
1. 文件路径正确
2. 使用绝对路径或相对路径
3. 文件名包含扩展名 `.json`

### ❓ 图片文件太大？

PNG 格式保证了高质量。可以使用以下工具压缩：
- [TinyPNG](https://tinypng.com/)
- [Squoosh](https://squoosh.app/)

### ❓ 能只生成 B30 或只生成 R10 吗？

可以手动编辑导出的 JSON 文件，删除不需要的部分（`best30` 或 `recent10` 数组）。

---

## 自定义配置

如果想修改图片样式（颜色、字体大小等），编辑：

```
scripts/generate-b30r10-image.js
```

找到 `CONFIG` 对象进行修改：

```javascript
const CONFIG = {
  // 画布尺寸
  canvasWidth: 2400,
  canvasHeight: 3600,
  
  // 卡片布局
  cardWidth: 440,
  cardHeight: 380,
  
  // 颜色（支持十六进制或rgba）
  colors: {
    background: '#1a1a2e',
    primary: '#667eea',
    // ... 更多颜色
  },
  
  // 字体大小
  fontSize: {
    playerName: 48,
    cardTitle: 28,
    // ... 更多字体
  }
};
```

---

## 下一步

想了解更多？查看：
- [完整文档](../IMAGE_GENERATOR_README.md)
- [示例数据格式](example-data.json)
- [主项目 README](../README.md)

---

**享受你的 Arcaea 成绩图片！** 🎮✨
